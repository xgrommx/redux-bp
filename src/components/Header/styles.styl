@keyframes bounce {
  0% {
    transform: scale(1)
  }
  70% {
    transform: scale(1.3)
  }
  100% {
    transform: scale(1)
  }
}


$beige = rgba(235, 228, 229, 0.91)
$blue = #000080

.logo
  text-align center
  font-size 1.5em
  padding 1.5em 0

.red
  margin-right -20px
  font-size 220px
  font-weight 800
  color red
  opacity .6

.blue
  margin-left -20px
  font-size 220px
  font-weight 900
  color blue
  opacity .6